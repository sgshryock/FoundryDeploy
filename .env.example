# Option A (recommended): Foundry account creds so the container can download/install
FOUNDRY_USERNAME=your_foundry_username
FOUNDRY_PASSWORD=your_foundry_password

# Foundry admin access key for *this* server instance
FOUNDRY_ADMIN_KEY=your_admin_key

# Foundry version (release = latest, or specify major version like 12, 13)
FOUNDRY_DOCKER_TAG=release

# Optional quality-of-life
CONTAINER_CACHE=/data/container_cache
# If you want config changes made in the Foundry UI to persist across restarts:
CONTAINER_PRESERVE_CONFIG=true

TZ=America/New_York

# Hostname for accessing Foundry in browser (can be custom)
FOUNDRY_HOSTNAME=your_hostname

# System hostname for Docker container (auto-detected by setup script)
# This must match the server's actual hostname to prevent license re-acknowledgement
SYSTEM_HOSTNAME=your_server_hostname

# Optional: Direct download URL (bypasses Foundry API - avoids rate limits)
# Get this from foundryvtt.com -> Purchased Licenses -> Download (timed link)
# FOUNDRY_RELEASE_URL=https://...

# =============================================================================
# Advanced: Environment Overrides (usually auto-detected)
# =============================================================================

# Environment type override (auto = auto-detect)
# Options: auto, physical, proxmox_vm, proxmox_lxc, aws_ec2
DEPLOY_ENVIRONMENT=auto

# Service manager override (auto = auto-detect)
# Options: auto, systemd, direct
DEPLOY_SERVICE_MANAGER=auto
