# Option A (recommended): Foundry account creds so the container can download/install
FOUNDRY_USERNAME=your_foundry_username
FOUNDRY_PASSWORD=your_foundry_password

# Foundry admin access key for *this* server instance
FOUNDRY_ADMIN_KEY=your_admin_key

# Optional quality-of-life
CONTAINER_CACHE=/data/container_cache
# If you want config changes made in the Foundry UI to persist across restarts:
CONTAINER_PRESERVE_CONFIG=true

TZ=America/New_York

# Hostname for the Foundry server
FOUNDRY_HOSTNAME=your_hostname
